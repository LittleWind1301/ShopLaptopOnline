
@{
    ViewBag.Title = "Trang chủ";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var slides = (List<ModelWeb.EF1.Slide>)ViewBag.Slides;
    var listNewProducts = (List<ModelWeb.EF1.tProduct>)ViewBag.NewProducts;
}

@section slide{
    <div class="header_slide">
        <div class="header_bottom_left">
            <div class="categories">
                @{Html.RenderAction("CategoryProduct", "Product");}
            </div>
            <div>
                @{Html.RenderAction("Filler", "Home");}
            </div>
        </div>
        <div class="header_bottom_right">
            @*<div class="slider">
            <div id="slider">
                <div id="mover">
                    @foreach (var item in slides)
                    {
                        <div class="slide">
                            <div class="slider-img">
                                <a href="@item.Link"><img src="@item.Image" alt="learn more" /></a>
                            </div>
                            <div class="slider-text">
                                <h1>Clearance<br><span>SALE</span></h1>
                                <h2>UPTo 20% OFF</h2>
                                <div class="features_list">
                                    <h4>Get to Know More About Our Memorable Services Lorem Ipsum is simply dummy text</h4>
                                </div>
                                <a href="preview.html" class="button">Shop Now</a>
                            </div>
                            <div class="clear"></div>
                        </div>
                    }

                </div>
            </div>
            <div class="clear"></div>
        </div>*@
            @{ Html.RenderAction("Slider", "Home");}
          

        </div>
        <div class="clear"></div>
    </div>
}
<div class="content">
    <div class="content_top">
        <div class="heading">
            <h3>Sản phẩm mới</h3>
        </div>
        <div class="see">
            @*<p><a href="/san-pham">Tất cả sản phẩm</a></p>*@
        </div>
        <div class="clear"></div>
    </div>
    <div class="section group">
        @if (listNewProducts.Count == 0)
        {
            <br />
            <span class="alert alert-danger">Không tìm thấy sản phẩm phù hợp</span>
        }
        @foreach (var item in listNewProducts)
        {
            var url = "/chi-tiet/" + item.MetaTittle + "-" + item.ID;
            <div class="grid_1_of_4 images_1_of_4">
                <a href="@url"><img src="@item.Image" alt="@item.Name" /></a>
                <h2 style="width: 100%; height: 100px; font-size: 14px">@item.Name </h2>
                <div class="price-details">
                    <div class="price-number">
                        <p><span class="rupees">&nbsp; &nbsp;&nbsp; @item.Price.GetValueOrDefault(0).ToString("N0")đ</span></p>
                    </div>
                    <div class="add-cart">
                        <h4><a href="/them-gio-hang?productId=@item.ID&quantity=1">Thêm vào giỏ hàng</a></h4>
                    </div>
                    <div class="clear"></div>
                </div>
            </div>
        }
    </div>
</div>
